name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: jubasjl76-eng/smart-pet-ci/.github/workflows/node-ci.yml@main
    with:
      node-version: "22"
      test-command: "npm test"
      run-build: true
